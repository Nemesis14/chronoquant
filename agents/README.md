# Agent Definitions

This directory contains role-specific agent definitions and skill scopes.

These files are not general project documentation. General engineering rules live
under `docs/engineering/`, stable architecture notes live under
`docs/architecture/`, domain workflow docs live under `docs/modeling/` and
`docs/evaluation/`, and implementation plans live under `docs/plans/`.
