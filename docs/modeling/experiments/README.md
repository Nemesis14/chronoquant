# Modeling Experiments

Use this folder for durable experiment summaries that are broader than one
model card. Raw trial logs and generated artifacts stay under `models/`.

Recommended experiment summary fields:

- question;
- data/sample;
- candidates compared;
- validation windows;
- key results;
- decision;
- follow-up tasks.

