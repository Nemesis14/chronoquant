# Active Plans

Use this folder for implementation plans that are currently being worked.

Only active work belongs here. When the task is finished, move the plan to
`docs/plans/completed/` and remove the related row from `board.md`.

