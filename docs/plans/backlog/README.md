# Backlog Plans

Use this folder for future implementation plans that are not active yet.

When a backlog item becomes active, move or copy the relevant plan to
`docs/plans/active/` and add the task to `board.md`.

