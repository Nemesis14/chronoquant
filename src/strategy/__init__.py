"""ChronoQuant strategy module.

CLI entry points:
    00_run_strategy_session.py  — Full session: scored join -> calibrate -> grid search.

Library:
    strategy/  — Core logic modules (build_table, calibrate, search, artifacts).
"""
